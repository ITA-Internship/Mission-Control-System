"""Configure the Django application for drone inventory management."""

from django.apps import AppConfig


class DronesConfig(AppConfig):
    """Configure the drones Django application."""

    name = "drones"
